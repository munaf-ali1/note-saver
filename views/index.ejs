<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block  w-full outline-none mt-5px rounded-md px-5 mt-5 py-5 bg-zinc-800" type="text" placeholder="Title goes" name="title">
                <textarea class="block w-full outline-none rounded-md px-5 py-5 mt-5 resize-none bg-zinc-800" placeholder="Write your tasks details here ... " name="details"></textarea>
                <input class="mt-5 px-5 py-5 rounded-md bg-blue-800"type="submit" value="Create tasks">
            </form>
        </div>
        <div class="tasks p-10 flex gap-5 ">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-2xl"><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-2" href="#">Read More</a>   
                    </div>
                <% }) %>
            <% } else { %>
                <div>
                    <h1 class="text-white bg-blue-500 p-5 rounded-md">No Tasks Done Yet...</h1>
                </div>
            <% } %>
            
          
            

        

        </div>

    </div>
    




    <script src="/javascript/index.js"></script>
</body>
</html>